import 'package:flutter/material.dart';

class DataAnalysisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Data Analysis")),
      body: Center(child: Text("Data Analysis Page")),
    );
  }
}
